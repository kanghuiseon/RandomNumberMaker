import XCTest
@testable import RandomNumberMaker

final class RandomNumberMakerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(RandomNumberMaker().text, "Hello, World!")
    }
}
